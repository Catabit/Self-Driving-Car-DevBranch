ARCH:=arm
CROSS_COMPILE:=$(CROSS)

CC=$(CROSS_COMPILE)gcc

obj-m += vdmadriver.o

KDIR := /home/catabit/Zybo/SDCar/build/tmp/work-shared/plnx_arm/kernel-source
PWD := $(shell pwd)


# make oldconfig && make prepare && make scripts && make tools

all:
	make default

default:
	${MAKE} -C ${KDIR} M=${PWD} modules CONFIG_STACK_VALIDATION=
	#${MAKE}  -C $(KERNEL) M=$(PWD) modules CONFIG_STACK_VALIDATION=

clean:
	${MAKE} -C ${KDIR} M=${PWD} clean