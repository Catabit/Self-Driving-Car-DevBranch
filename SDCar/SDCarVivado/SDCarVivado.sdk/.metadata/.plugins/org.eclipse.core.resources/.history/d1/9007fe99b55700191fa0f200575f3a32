#include "PN532_rfid.h"


int main() {
	int fd = initRFID();
	if (fd < 0)
		return -1;

	struct cardQueue *queue = NULL;
	createCardQueue(&queue);

	runRFID(fd, queue);

	printQueue(queue);

	freeCardQueue(queue);
	free(queue);

	close(fd);
}
